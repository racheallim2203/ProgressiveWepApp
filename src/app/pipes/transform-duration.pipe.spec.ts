import { TransformDurationPipe } from './transform-duration.pipe';

describe('TransformDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
