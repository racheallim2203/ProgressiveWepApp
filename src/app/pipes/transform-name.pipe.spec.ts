import { TransformNamePipe } from './transform-name.pipe';

describe('TransformNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformNamePipe();
    expect(pipe).toBeTruthy();
  });
});
